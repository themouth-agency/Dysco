import React from 'react';
import { View } from 'react-native';

export const ModeSwitcher: React.FC = () => {
  // This component is now just a placeholder
  // Role selection happens in the WelcomeScreen
  return <View style={{ display: 'none' }} />;
}; 